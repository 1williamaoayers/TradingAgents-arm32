#!/bin/bash

# ============================================
# 文件索引 - 快速查找脚本
# ============================================

cat << 'EOF'

╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║              Google News 脚本集合 - 文件索引                 ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

📂 google_news_scripts/
│
├── 📄 使用说明.md                    中文小白版说明(推荐先看这个)
├── 📄 README.md                      英文详细文档
├── 📄 QUICK_START.sh                 交互式快速启动菜单
├── 📄 INDEX.sh                       本文件索引
│
├── 📁 test/                          测试脚本文件夹
│   ├── test_basic.sh                 基础测试(8项检查)
│   ├── test_with_dns_fix.sh          DNS自动修复测试
│   ├── test_complete.sh              完整测试(推荐)⭐
│   └── test_python.py                Python环境测试
│
└── 📁 tools/                         实用工具文件夹
    ├── get_news.sh                   一键获取新闻(最常用)⭐⭐⭐
    └── simple_news.py                Python新闻工具

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 快速导航:

  【新手入门】
    1. 先看: 使用说明.md
    2. 运行: bash test/test_complete.sh
    3. 使用: bash tools/get_news.sh

  【遇到问题】
    DNS问题  → bash test/test_with_dns_fix.sh
    环境测试 → bash test/test_complete.sh
    Python   → python3 test/test_python.py

  【日常使用】
    获取新闻 → bash tools/get_news.sh
    Python   → python3 tools/simple_news.py
    菜单模式 → bash QUICK_START.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 脚本对比:

┌─────────────────────┬──────────┬──────────┬─────────────┐
│ 脚本名称            │ 难度     │ 推荐度   │ 用途        │
├─────────────────────┼──────────┼──────────┼─────────────┤
│ get_news.sh         │ ⭐       │ ⭐⭐⭐   │ 日常获取    │
│ simple_news.py      │ ⭐⭐     │ ⭐⭐⭐   │ 功能丰富    │
│ test_complete.sh    │ ⭐       │ ⭐⭐⭐   │ 完整测试    │
│ test_with_dns_fix.sh│ ⭐       │ ⭐⭐     │ DNS修复     │
│ test_basic.sh       │ ⭐       │ ⭐⭐     │ 快速测试    │
│ test_python.py      │ ⭐⭐     │ ⭐⭐     │ Python测试  │
│ QUICK_START.sh      │ ⭐       │ ⭐⭐⭐   │ 交互菜单    │
└─────────────────────┴──────────┴──────────┴─────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 使用建议:

  小白用户:
    → 直接用 get_news.sh,最简单!
    → 或者用 QUICK_START.sh 交互式菜单

  有Python基础:
    → 用 simple_news.py,可以自定义

  遇到问题:
    → 先运行 test_complete.sh 诊断
    → DNS问题用 test_with_dns_fix.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 文件说明:

  使用说明.md
    → 中文小白版说明
    → 最简单易懂
    → 推荐新手先看

  README.md
    → 英文详细文档
    → 包含所有细节
    → 适合深入了解

  QUICK_START.sh
    → 交互式菜单
    → 不用记命令
    → 输入数字选择

  INDEX.sh
    → 本文件索引
    → 快速查找脚本

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 最常用的3个命令:

  1. 测试环境:
     bash test/test_complete.sh

  2. 获取新闻:
     bash tools/get_news.sh

  3. 修复DNS:
     bash test/test_with_dns_fix.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EOF

echo ""
echo "需要查看某个文件的详细说明吗?"
echo ""
echo "  1) 查看使用说明.md"
echo "  2) 查看README.md"
echo "  3) 运行快速启动菜单"
echo "  0) 退出"
echo ""
read -p "请输入选项 [0-3]: " choice

case $choice in
    1)
        if [ -f "使用说明.md" ]; then
            cat 使用说明.md
        else
            echo "文件不存在"
        fi
        ;;
    2)
        if [ -f "README.md" ]; then
            cat README.md
        else
            echo "文件不存在"
        fi
        ;;
    3)
        bash QUICK_START.sh
        ;;
    0)
        echo "退出"
        exit 0
        ;;
    *)
        echo "无效选项"
        exit 1
        ;;
esac
